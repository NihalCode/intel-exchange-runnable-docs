# Responsive

| Breakpoint | Behavior |
|---|---|
| Mobile | Drawer navigation, stacked hub search + Ask AI, single-column product modules → 2-col |
| Tablet | Product constellation 2-col, workflow ribbon 3-col from `md`, access/auth forms centered |
| Desktop | Header zones, product strip with Ctrl+K hint, docs left rail, ask 3-pane workspace |
| Wide | Hub max width, analytics 4-col telemetry, TOC rail at `xl` |

Home hero signal art is desktop-only (`lg:block`) to protect mobile clarity.

Reduced motion: global CSS disables atmosphere/pulse/trace animations.
